version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: mrs-db
    environment:
      POSTGRES_USER: mrssystem
      POSTGRES_PASSWORD: mrs123
      POSTGRES_DB: mrssystem
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U mrssystem"]
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile.dev
    container_name: mrs-backend
    ports:
      - "19080:8080"
    environment:
      DATABASE_URL: ${DATABASE_URL}
      JWT_SECRET: ${JWT_SECRET}
      PORT: ${PORT}
      GIN_MODE: ${GIN_MODE}
      TMDB_API_KEY: ${TMDB_API_KEY}
      PLEX_SERVER_URL: ${PLEX_SERVER_URL}
      PLEX_TOKEN: ${PLEX_TOKEN}
    volumes:
      - ./backend:/app
      - /app/tmp
    depends_on:
      postgres:
        condition: service_healthy
    command: air

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile.dev
    container_name: mrs-frontend
    ports:
      - "19173:5173"
    environment:
      - VITE_API_URL=${VITE_API_URL}
    volumes:
      - ./frontend:/app
      - /app/node_modules
    depends_on:
      - backend

volumes:
  postgres_data:
